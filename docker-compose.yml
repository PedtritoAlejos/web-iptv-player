version: '3.8'

services:
  php-proxy:
    image: php:8.2-apache
    ports:
      - "8000:80"
    volumes:
      - ./public:/var/www/html
    environment:
      - APACHE_DOCUMENT_ROOT=/var/www/html
    networks:
      - iptv-network

networks:
  iptv-network:
    driver: bridge
